# Gerado na migração 2026-08-17 — NÃO colocar valores aqui.
# Restaurar: op inject -i .env.local.tmpl -o .env.local
# (ou o arquivo inteiro: op document get "max-agent.doc" --vault dev --out-file .env.local)
DATABASE_URL=op://dev/max-agent/DATABASE_URL
